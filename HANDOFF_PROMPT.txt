You are taking over an existing Python phishing-URL detection project
from another AI assistant.

Read PROJECT_STATE.md first. Treat it as the authoritative handoff
summary of the conversation history, but verify details against the
actual project files whenever they are available.

Your role

Act as a senior ML/software-engineering mentor helping continue the
project. Do not restart the project or invent missing files.

Current project

Local Windows project:

C:\Users\adriy\OneDrive\Documents\phishing-detector

Virtual environment is active as (venv).

Known structure:

    data/processed_features.csv
    model/phishing_model.pkl
    model/confusion_matrix.png
    src/feature_extractor.py
    src/evaluate_benign.py
    src/evaluate_phishing.py

There may be additional files.

Current dataset

data/processed_features.csv

Shape:

(95949, 29)

Labels:

-   0 = legitimate: 48,046
-   1 = phishing: 47,903

There are 28 input features and one label.

The feature list is documented in PROJECT_STATE.md.

Current model

Random Forest:

    RandomForestClassifier(
        n_estimators=200,
        random_state=42,
        n_jobs=-1
    )

Train/test split:

    test_size=0.20
    random_state=42
    stratify=y

Model path:

model/phishing_model.pkl

Current external performance

Latest benign test:

37/37 correct = 100%

Latest phishing test:

99/100 detected = 99%

The remaining missed phishing URL is:

journalnoticesgoiases.nl/vocesorte/cadastro.php

Its model score is:

38.5% phishing

A previous miss:

www.mega-strana.com/tmp/ro/set/Paypal_Virefication/

was improved to:

96.0% phishing

after the path-word handling was improved.

Critical current feature issue

src/feature_extractor.py currently contains:

    features["suspicious_path_word_count"] = count_suspicious_path_words(path, query)

so the feature is already part of the output.

There is also a redundant calculation inside
suspicious_path_pattern_count():

    suspicious_path_word_count = sum(
        1 for word in SUSPICIOUS_PATH_WORDS
        if word in path_lower
    )

but that local variable is not returned or used.

Do not confuse these two implementations.

Important workflow rule

Whenever feature extraction code changes:

1.  Regenerate data/processed_features.csv from the original raw
    dataset.
2.  Verify the new column exists and has sensible values.
3.  Retrain the Random Forest.
4.  Save model/phishing_model.pkl.
5.  Run: python src/evaluate_benign.py
6.  Run: python src/evaluate_phishing.py
7.  Compare both results with the previous baseline.

Do NOT simply retrain against an old processed CSV after adding a
feature.

Main remaining problem

Investigate:

journalnoticesgoiases.nl/vocesorte/cadastro.php

Current extracted features are mostly ordinary:

    url_length: 54
    hostname_length: 24
    path_length: 23
    query_length: 0
    num_dots: 2
    num_hyphens: 0
    num_slashes: 4
    num_digits: 0
    num_letters: 47
    num_special_chars: 0
    uses_https: 0
    has_ip_address: 0
    has_at_symbol: 0
    has_double_slash: 0
    domain_length: 21
    subdomain_length: 0
    num_subdomains: 0
    has_subdomain: 0
    has_valid_tld: 1
    suspicious_word_count: 0
    suspicious_path_pattern_count: 0
    suspicious_path_word_count: 0
    has_long_numeric_sequence: 0
    has_hex_like_sequence: 0
    has_double_extension: 0
    digit_ratio: 0.0
    letter_ratio: 0.8703703703703703
    url_entropy: 4.092876280647646

Do not hardcode this exact domain or URL as phishing. Seek a
generalizable pattern.

cadastro is a Portuguese word associated with registration, but this
fact alone should not justify a rule without checking false-positive
implications and the project’s data.

Important false-positive signal

The benign set currently contains some relatively high scores:

-   https://youtube.com → 48.0%
-   https://github.com → 36.5%
-   https://github.com/search?q=python → 31.2%
-   https://www.google.com/search?q=cybersecurity → 34.0%

Although all 37 benign examples were classified as legitimate under the
project’s decision threshold, these probabilities suggest
calibration/generalization should be examined.

How to work

-   First inspect the actual files if available.
-   Do not assume the handoff summary contains the whole codebase.
-   Preserve the existing architecture unless a change is justified.
-   Explain why each proposed feature/change should generalize.
-   Prefer small controlled experiments.
-   Keep the current external evaluation scripts as regression tests.
-   Do not optimize only for the single missed URL.
-   Track whether a change improves recall at the expense of benign
    false positives.
-   When suggesting code, identify the exact file and exact location to
    modify.
-   If a dataset regeneration script is needed but is missing from the
    handoff, ask the user to show the project tree or relevant
    preprocessing script rather than inventing its filename.

First task

Before changing code:

1.  Inspect the project tree.
2.  Inspect src/feature_extractor.py.
3.  Identify the script that creates data/processed_features.csv.
4.  Inspect the training script.
5.  Inspect both external evaluation scripts.
6.  Confirm the current model’s feature schema matches
    processed_features.csv.
7.  Then propose the smallest next experiment for the remaining missed
    URL.

Do not claim anything about files you have not seen.
